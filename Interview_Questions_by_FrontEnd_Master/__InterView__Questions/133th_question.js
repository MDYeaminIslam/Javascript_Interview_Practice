const prompt = require("prompt-sync")();

console.log(+false); //here + operator will convert the boolean value into number and then print the value.We know that false is 0 and true is 1.
console.log(!"Javascript"); //here ! operator will convert the string into boolean and then print the value.We know that any string is true except empty string.